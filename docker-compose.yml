services:
  aios:
    build: .
    container_name: aios
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    environment:
      - PORT=${PORT:-8080}
      - SANDBOX_DIR=${SANDBOX_DIR:-/app/workspace}
    volumes:
      - ./workspace:/app/workspace
